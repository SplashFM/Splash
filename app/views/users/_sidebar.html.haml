#side-bar-wrap.left
  #side-bar
    = render :partial => "users/vcard", :locals => {:user => current_user}

    = render 'users/follow_box'

    .ripples{:data => {:widget => 'ripples'}}
      %span.label RIPPLES
      %span.number= @user.ripple_count

    .splashes
      %span.label SPLASHES
      %span.number= 777
    / .influence top 5% influencer this week
    / .last-seen last seen 8 min ago

    .splashers-widgets
      .top-splashes
        .tabs
          %ul
            %li
              %a.active{:href => "#"} Top Splashes
            %li
              %a{:href => "#"} Top Splashers
          .line
            %span
          %a.toggle{:href => "#"}
        %ul#splashes.tab
          = render "splashes/top_splash"
          = render "splashes/top_splash"
          = render "splashes/top_splash"
          = render "splashes/top_splash"
          = render "splashes/top_splash"
          = render "splashes/top_splash"
          = render "splashes/top_splash"
          = render "splashes/top_splash"
        %a.viewmore-btn{:href => "#"} View Splashboard
    = render "shared/invites"
