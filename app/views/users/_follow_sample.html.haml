.avatars
  - @user.send("#{type}_sample", 3).each do |user|
    = link_to (image_tag user.avatar(:thumb), :size => "32x32"), root_url() + user.slug
.text
  = link_to_relationship(@user.send(type).count, type)

