:javascript
  $(function () {
    var topTracks = new Splashboard.Items({
      template: '#tmpl-home-track',
      feed: new TrackList,
      app: window.App,
      extraClass: 'live-feed'
    })
    $('#tab-top-tracks').html(topTracks.el)

    var topUsers = new Splashboard.Items({
      template: '#tmpl-user',
      feed: new UserList,
      app: window.App,
      extraClass: 'live-feed'
    })
    $('#tab-top-users').html(topUsers.el)
  });

.splashboard
  = render "home/sidebar"
  #stream-wrap.left.tabs
    #stream-feed
      .streamfeed-top
        .main-panel
          %label.avan-demi
            Splashboard
        .feed-settings
          .avan-bold
            %ul
              %li
                %a{:href => "#tab-top-tracks"} Most Splashed
              %li
                %a{:href => "#tab-top-users"} Top Splashers
          .line
            %span
      =# render "shared/feed_filter"
      #tab-top-tracks
      #tab-top-users
      .loading-spinner
    .cl &nbsp;

