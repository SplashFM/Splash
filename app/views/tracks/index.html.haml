- content_for :head do
  = include_javascripts :soundcloud

= form_tag tracks_path, {:method => :get} do
  = text_field_tag :f, params[:f]
  = submit_tag 'Search'

- if @results
  - if @results.empty?
    = t '.empty'
  - else
    %ul.tracks
      - @results.each do |r|
        %li= r.title
