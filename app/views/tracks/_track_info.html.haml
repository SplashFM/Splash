%div{:data => {:widget => 'track-info'}}
  Expanded!

  - if track.downloadable?
    = link_to t('.download'), download_track_path(track)

  - elsif track.purchasable?
    = link_to t(".purchase"), track.purchase_url