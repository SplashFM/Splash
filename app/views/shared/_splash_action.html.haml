%div{:data => {:widget => 'full-splash-action'}}
  .splash-action
    %a{:class => '{{if !splashable}}unsplashIcon icon splash-btn{{else}}icon splash-btn{{/if}}', :href => "#", :data => {:widget => 'toggle-splash'}}
    {{if splashable}}  
    %span.splash Splash!
    {{else}}  
    %span.unsplash Oops!
    {{/if}}
  
  - if current_user
    = render 'shared/splash_form'
  - else
    %form
      {{html JST['shared/login_required_to_splash']()}}
